// Fill these in after creating your free Supabase project (see README.md).
// SUPABASE_URL looks like: https://xxxxxxxxxxxx.supabase.co
// SUPABASE_ANON_KEY is the long "anon public" key from Project Settings > API.
// Both are safe to have in this public file — they only work within the
// permissions ("policies") set up by the SQL script in supabase/schema.sql.

const SUPABASE_URL = "YOUR_SUPABASE_URL";
const SUPABASE_ANON_KEY = "YOUR_SUPABASE_ANON_KEY";
